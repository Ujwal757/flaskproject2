{
  "rules": {
    "users": {
      ".indexOn": ["email"],
      "$userId": {
        ".read": "true",
        ".write": "true"
      }
    },
    "reports": {
      ".read": "true",
      ".write": "true",
      "$reportId": {
        ".indexOn": ["severity", "status", "assignedWorkerId", "timestamp"]
      }
    }
  }
}

